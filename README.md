# robot_deployment

```
mkdir ~/src
git clone -b lm_devel https://github.com/ADVRHumanoids/robot_deployment.git

cd ~/src/robot_deployment

sudo dpkg -i xenomai/dovetail/*.deb

./xenomai/dovetail/prepare-dovetail.sh

```

